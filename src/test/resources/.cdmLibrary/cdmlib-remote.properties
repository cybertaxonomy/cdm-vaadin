#
# The properties defined herein are loaded by WebApplicationConfigurers
# see AbstractWebApplicationConfigurer in cdmlib-remote
#

# the datasource bean to use

# no need to set cdm.datasource=h2_cdmTest here since it is overwritten anyway in selenide-integration-test.properties
#cdm.datasource=h2_cdmTest 
